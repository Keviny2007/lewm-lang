dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: tworoom_language
  keys_to_load:
    - pixels
    - action
    - proprio
    - language_emb
  keys_to_cache:
    - action
    - proprio
    - language_emb
