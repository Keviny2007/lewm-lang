dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: pusht_expert_train_language
  keys_to_load:
    - pixels
    - action
    - proprio
    - state
    - language_emb
  keys_to_cache:
    - action
    - proprio
    - state
    - language_emb
