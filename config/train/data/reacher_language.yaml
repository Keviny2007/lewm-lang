dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 5
  name: reacher_language
  keys_to_load:
    - pixels
    - action
    - observation
    - language_emb
  keys_to_cache:
    - action
    - observation
    - language_emb
