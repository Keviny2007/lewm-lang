dataset:
  num_steps: ${eval:'${wm.num_preds} + ${wm.history_size}'}
  frameskip: 2
  name: language_table_blocktoblock_4block_sim
  keys_to_load:
    - pixels
    - action
    - language_emb
  keys_to_cache:
    - action
    - language_emb
